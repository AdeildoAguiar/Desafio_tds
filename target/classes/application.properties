spring.application.name=Desafio_tds

# Configura��es do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/tds
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configura��o de log para depura��o
logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG

# Configura��o do dialeto do Hibernate para MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configura��o para criar as tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update
